
 
 on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
#following jobs will run after above github event happens 
 
 jobs:
  build:
    
	runs-on: self-hosted
	
	steps:
    - uses: actions/checkout@v3
	
	- name: Build with Maven
      run: mvn -B package --file pom.xml 
